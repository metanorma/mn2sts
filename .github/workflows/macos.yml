name: macos

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@master

      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      # cabextract for fonts
      - name: Setup prerequisites
        run: |
          # Install Maven
          brew install maven

      - name: Run tests
        run: |
          make test

      - name: Test against XSD NISO
        run: |
          make mn2stsXSD

      - name: Test against DTD NISO
        run: |
          make mn2stsDTD_NISO

      - name: Test against DTD ISO
        run: |
          make mn2stsDTD_ISO

      - name: Build
        run: |
          make all

      - name: Compile output
        run: |
          make publish
